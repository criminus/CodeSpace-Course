{# navigation.twig #}
<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ pages.index.url }}">
            <i class="bi bi-watch"></i> MK Time </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if pages.index.isActive %}active{% endif %}" aria-current="page" href="{{ pages.index.url }}">
                        <i class="bi bi-house-door-fill"></i> Home </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if pages.mens.isActive %}active{% endif %}" aria-current="page" href="{{ pages.mens.url }}">
                        <i class="bi bi-bag-check"></i> Products </a>
                </li>    
{#                 <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {% if pages.mens.isActive or pages.pocket.isActive or pages.womens.isActive %}active{% endif %}" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-bag-check"></i> Products </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item {% if pages.mens.isActive %}active{% endif %}" href="{{ pages.mens.url }}">Mens Watch</a>
                        </li>
                        <li>
                            <a class="dropdown-item {% if pages.womens.isActive %}active{% endif %}" href="{{ pages.womens.url }}">Womens Watch</a>
                        </li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <a class="dropdown-item {% if pages.pocket.isActive %}active{% endif %}" href="{{ pages.pocket.url }}">Pocket Watch</a>
                        </li>
                    </ul>
                </li> #}
                <li class="nav-item">
                    <a class="nav-link {% if pages.contact.isActive %}active{% endif %}" href="{{ pages.contact.url }}">
                        <i class="bi bi-person-lines-fill"></i> Contact </a>
                </li>
            </ul>

            {% if user.userLoggedIn %}
            <span class="navbar-text">
                <hr class="responsive-hr" /> {{ user.email }}
            </span>
            <span class="navbar-text">
                <form class="container-fluid justify-content-start  responsive-cart">
                    <button type="button" class="btn btn-light position-relative">
                        <i class="bi bi-cart-plus"></i> Cart <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"> 0 <span class="visually-hidden"></span>
                        </span>
                    </button>
                </form>
            </span>
            <span class="navbar-text">
                <a href="logout.php" class="btn btn-sm btn-danger" type="button">
                    <i class="bi bi-box-arrow-right"></i> Log out </a>
            </span>
            {% else %}
            <span class="navbar-text">
                <form class="container-fluid justify-content-start">
                    <a href="{{ pages.signup.url }}" class="btn {% if pages.signup.isActive %}btn-success{% else %}btn-outline-success{% endif %} me-2" id="signup" data-bs-toggle="tooltip" data-bs-placement="left" data-bs-title="New to MK Time? Create a free account now!" type="button">Sign Up</a>
                    <a href="{{ pages.signin.url }}" class="btn {% if pages.signin.isActive %}btn-sm btn-secondary{% else %}btn-sm btn-outline-secondary{% endif %}" type="button">Sign In</a>
                </form>
            </span>
            {% endif %}
        </div>
    </div>
</nav>